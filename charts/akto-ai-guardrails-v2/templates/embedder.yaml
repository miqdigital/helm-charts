{{- if .Values.embedder.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "akto-ai-guardrails-v2.fullname" . }}-embedder
  labels:
    {{- include "akto-ai-guardrails-v2.componentLabels" (list . "embedder") | nindent 4 }}
spec:
  replicas: {{ .Values.embedder.replicas }}
  selector:
    matchLabels:
      {{- include "akto-ai-guardrails-v2.componentSelectorLabels" (list . "embedder") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "akto-ai-guardrails-v2.componentSelectorLabels" (list . "embedder") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "akto-ai-guardrails-v2.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: embedder
        securityContext:
          {{- toYaml .Values.securityContext | nindent 10 }}
        image: "{{ .Values.embedder.image.repository }}:{{ .Values.embedder.image.tag }}"
        imagePullPolicy: {{ .Values.embedder.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.embedder.service.targetPort }}
          protocol: TCP
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: PORT
          value: {{ quote .Values.embedder.service.targetPort }}
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        {{- if .Values.embedder.livenessProbe.enabled }}
        livenessProbe:
          httpGet:
            path: {{ .Values.embedder.livenessProbe.httpGet.path }}
            port: {{ .Values.embedder.livenessProbe.httpGet.port }}
          initialDelaySeconds: {{ .Values.embedder.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.embedder.livenessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.embedder.livenessProbe.timeoutSeconds }}
          failureThreshold: {{ .Values.embedder.livenessProbe.failureThreshold }}
        {{- end }}
        {{- if .Values.embedder.readinessProbe.enabled }}
        readinessProbe:
          httpGet:
            path: {{ .Values.embedder.readinessProbe.httpGet.path }}
            port: {{ .Values.embedder.readinessProbe.httpGet.port }}
          initialDelaySeconds: {{ .Values.embedder.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.embedder.readinessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.embedder.readinessProbe.timeoutSeconds }}
          failureThreshold: {{ .Values.embedder.readinessProbe.failureThreshold }}
        {{- end }}
        resources:
          {{- toYaml .Values.embedder.resources | nindent 10 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "akto-ai-guardrails-v2.fullname" . }}-embedder
  labels:
    {{- include "akto-ai-guardrails-v2.componentLabels" (list . "embedder") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.embedder.service.port }}
    targetPort: {{ .Values.embedder.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "akto-ai-guardrails-v2.componentSelectorLabels" (list . "embedder") | nindent 4 }}
{{- end }}
