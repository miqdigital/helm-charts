apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "akto.fullname" . }}-database-abstractor
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll
    keel.sh/pollSchedule: "@every 60m"
  labels:
  {{- include "akto.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.database_abstractor.replicas }}
  selector:
    matchLabels:
      app: {{ include "akto.fullname" . }}-database-abstractor
    {{- include "akto.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: {{ include "akto.fullname" . }}-database-abstractor
      {{- include "akto.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: AKTO_ACCOUNT_NAME
          value: {{ quote .Values.database_abstractor.aktoApiSecurityDatabaseAbstractor.env.aktoAccountName
            }}
        - name: IS_KUBERNETES
          value: "true"
        - name: AKTO_CONFIG_NAME
          value: {{ quote .Values.database_abstractor.aktoApiSecurityDatabaseAbstractor.env.aktoConfigName
            }}
        - name: AKTO_INSTANCE_TYPE
          value: {{ quote .Values.database_abstractor.aktoApiSecurityDatabaseAbstractor.env.aktoInstanceType
            }}
        - name: AKTO_MONGO_CONN
          {{- if .Values.mongo.useKeyVault }}
          valueFrom:
            secretKeyRef:
              name: {{ .Values.mongo.secretName }}
              key: {{ .Values.mongo.secretKey | default "aktoMongoConn" }}
          {{- else }}
          value: {{ quote .Values.mongo.aktoMongoConn }}
          {{- end }}
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        - name: SEARCH_BACKEND
          value: {{ quote .Values.elasticsearch.searchBackend }}
        - name: ES_HOST
          value: {{ quote .Values.elasticsearch.host }}
        - name: ES_INDEX_AGENT_QUERY
          value: {{ quote .Values.elasticsearch.indexAgentQuery }}
        - name: ES_API_KEY
          {{- if .Values.elasticsearch.useKeyVault }}
          valueFrom:
            secretKeyRef:
              name: {{ .Values.elasticsearch.secretName }}
              key: {{ .Values.elasticsearch.secretKey | default "esApiKey" }}
          {{- else }}
          value: {{ quote .Values.elasticsearch.apiKey }}
          {{- end }}
        image: {{ .Values.database_abstractor.aktoApiSecurityDatabaseAbstractor.image.repository }}:{{ .Values.database_abstractor.aktoApiSecurityDatabaseAbstractor.image.tag
          | default .Chart.AppVersion }}
        imagePullPolicy: {{ .Values.database_abstractor.aktoApiSecurityDatabaseAbstractor.imagePullPolicy
          }}
        name: akto-api-security-database-abstractor
        ports:
        - containerPort: 9000
        resources: {{- toYaml .Values.database_abstractor.aktoApiSecurityDatabaseAbstractor.resources |  nindent 10 }}
        {{- if or .Values.mongo.useKeyVault .Values.elasticsearch.useKeyVault }}
        volumeMounts:
        {{- if .Values.mongo.useKeyVault }}
        - name: secrets-store
          mountPath: /mnt/secrets-store
          readOnly: true
        {{- end }}
        {{- if .Values.elasticsearch.useKeyVault }}
        - name: secrets-store-es
          mountPath: /mnt/secrets-store-es
          readOnly: true
        {{- end }}
        {{- end }}
      {{- if or .Values.mongo.useKeyVault .Values.elasticsearch.useKeyVault }}
      volumes:
      {{- if .Values.mongo.useKeyVault }}
      - name: secrets-store
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: {{ .Values.mongo.secretProviderClass | default "akto-keyvault" }}
      {{- end }}
      {{- if .Values.elasticsearch.useKeyVault }}
      - name: secrets-store-es
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: {{ .Values.elasticsearch.secretProviderClass | default "akto-keyvault" }}
      {{- end }}
      {{- end }}
      restartPolicy: Always