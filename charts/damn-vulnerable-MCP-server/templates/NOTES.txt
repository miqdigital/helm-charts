*******************************************************************************
*** Damn Vulnerable MCP Server - Educational Security Testing Platform ***
*******************************************************************************

⚠️  WARNING: This deployment contains intentionally vulnerable servers for
    educational purposes only. DO NOT expose to production networks or the
    public internet!

Your release is named {{ .Release.Name }}.

To access the 10 challenge servers, you need to get the service endpoint:

{{- if eq .Values.service.type "NodePort" }}

1. Get the node IP:
   export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")

2. Get the node ports:
   kubectl get svc --namespace {{ .Release.Namespace }} {{ include "damn-vulnerable-mcp-server.fullname" . }}

3. Access the challenges via HTTP:
   Challenge 1:  http://$NODE_IP:<NODE_PORT_9001>
   Challenge 2:  http://$NODE_IP:<NODE_PORT_9002>
   Challenge 3:  http://$NODE_IP:<NODE_PORT_9003>
   Challenge 4:  http://$NODE_IP:<NODE_PORT_9004>
   Challenge 5:  http://$NODE_IP:<NODE_PORT_9005>
   Challenge 6:  http://$NODE_IP:<NODE_PORT_9006>
   Challenge 7:  http://$NODE_IP:<NODE_PORT_9007>
   Challenge 8:  http://$NODE_IP:<NODE_PORT_9008>
   Challenge 9:  http://$NODE_IP:<NODE_PORT_9009>
   Challenge 10: http://$NODE_IP:<NODE_PORT_9010>

{{- else if eq .Values.service.type "LoadBalancer" }}

1. Get the external IP (may take a few minutes):
   export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "damn-vulnerable-mcp-server.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")

   NOTE: It may take a few minutes for the LoadBalancer IP to be available.
   You can watch the status by running:
   kubectl get svc --namespace {{ .Release.Namespace }} -w {{ include "damn-vulnerable-mcp-server.fullname" . }}

2. Access the challenges via HTTP:
   Challenge 1:  http://$SERVICE_IP:9001
   Challenge 2:  http://$SERVICE_IP:9002
   Challenge 3:  http://$SERVICE_IP:9003
   Challenge 4:  http://$SERVICE_IP:9004
   Challenge 5:  http://$SERVICE_IP:9005
   Challenge 6:  http://$SERVICE_IP:9006
   Challenge 7:  http://$SERVICE_IP:9007
   Challenge 8:  http://$SERVICE_IP:9008
   Challenge 9:  http://$SERVICE_IP:9009
   Challenge 10: http://$SERVICE_IP:9010

{{- else if eq .Values.service.type "ClusterIP" }}

The service is only accessible within the cluster. To access it locally:

1. Port forward all challenge servers:
   kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "damn-vulnerable-mcp-server.fullname" . }} 9001:9001 9002:9002 9003:9003 9004:9004 9005:9005 9006:9006 9007:9007 9008:9008 9009:9009 9010:9010

2. Access the challenges via HTTP:
   Challenge 1:  http://localhost:9001
   Challenge 2:  http://localhost:9002
   Challenge 3:  http://localhost:9003
   Challenge 4:  http://localhost:9004
   Challenge 5:  http://localhost:9005
   Challenge 6:  http://localhost:9006
   Challenge 7:  http://localhost:9007
   Challenge 8:  http://localhost:9008
   Challenge 9:  http://localhost:9009
   Challenge 10: http://localhost:9010

Alternatively, change the service type to LoadBalancer or NodePort:
   helm upgrade {{ .Release.Name }} {{ .Chart.Name }} --set service.type=LoadBalancer

{{- end }}

Challenge Difficulty Levels:
  • Easy (1-3): Basic vulnerabilities like prompt injection
  • Medium (4-7): Tool shadowing, permission issues
  • Hard (8-10): Advanced multi-vector attacks

For MCP Client Configuration (Claude Desktop, Cursor, MCP Inspector):
  Configure each challenge as a separate server endpoint using the URLs above.

More Information:
  Project: https://github.com/harishsg993010/damn-vulnerable-MCP-server
  Documentation: See README for detailed challenge descriptions

Useful Commands:
  - View logs: kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "damn-vulnerable-mcp-server.name" . }}
  - Get pods: kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "damn-vulnerable-mcp-server.name" . }}
  - Delete release: helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

Happy (ethical) hacking! 🔒
