apiVersion: v1
kind: Service
metadata:
  name: {{ include "akto.fullname" . }}-protection
  labels:
  {{- include "akto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.runtime.type }}
  selector:
    app: {{ include "akto.fullname" . }}-protection
  {{- include "akto.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.runtime.ports | toYaml | nindent 2 -}}